'use strict';

const { $object, $entry } = require('./objects');
const { $array } = require('./arrays');
const { $block } = require('./blocks');

module.exports = {
  $object,
  $entry,
  $array,
  $block,
};
